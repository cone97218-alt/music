# FIRE (Fly In Rhythmic Expression) 音乐拓展 - 更新日志

## 🚀 v2.4.7 (2026-09-12)

### 📱 手机端「返回非标准JSON」与自检日志报错根治
- **解决酒馆中转接口 Gzip 响应在移动端的解压兼容（返回非标准JSON）**：
  - SillyTavern 后端代理接口 `/api/search/visit` 在转发网易云数据（如歌单搜索 >10KB）时，因 Express 与上游返回 Gzip 压缩字节流且未带标准解压标头，导致手机端 WebKit/Chromium 浏览器获取到的文本以 `0x1f 0x8b` 魔数开头，触发 `JSON.parse` 报错「返回数据非标准JSON格式」；
  - 新增 `parseResponseTextSafe` 响应安全流解析器，在二进制层级嗅探 Gzip (`0x1f 0x8b`) 及 Deflate (`0x78`) 压缩头，自动通过浏览器内置 `DecompressionStream` 管道流实时透明解压，完美修复移动端接收大体积歌单搜索数据时的乱码与解析失败；
  - 增加 UTF-8 BOM (`\uFEFF`) 自动剥离与 JSON 子串截取自愈逻辑，防止非标字符损坏。
- **修复自检工具 `addLog is not defined` 报错**：
  - 统一并补全日志记录器 `addLog` 与 `logInfo` 接口，解决设置面板内点击「手机接口自检」时由于日志函数未声明导致自检流程意外抛错中断的问题。
- **中文输入法不可见零宽字符过滤**：
  - 针对手机端第三方输入法（搜狗、百度、iOS 原生键盘）输入文字时偶发的零宽字符（如 `\u200B`、`\u200C`、`\u200D`、`\u00A0`），在发起搜索前做全面清理，确保检索关键词纯净。

## 🎛️ v2.4.6 (2026-09-12)

### 🎵 歌单界面「设置起播时间」与「加Tag」功能键设置为可选项
- **曲目操作按键自定义开关**：
  - 在播放器设置的「歌单导入与备份」板块中，新增「歌单曲目功能按键显示」个性化配置：
    - **显示「设置起播时间」按键**（默认开启）：关闭后隐藏曲目右侧的时钟图标及空起播时间标记，界面更加紧凑纯净；
    - **显示「加 Tag (标签)」按键**（默认开启）：关闭后隐藏曲目右侧的 Tag 标签图标及曲目下方的「＋标签」空占位条，彻底消除歌曲列表的视觉干扰。
- **动态自适应排版**：
  - 当歌曲既无起播时间也无标签时，整行标签栏自动隐藏，单曲高度更加纤细规整，大幅提升手机端与桌面端长歌单的浏览体验。

## 🩺 v2.4.5 (2026-09-12)

### 📱 手机端搜歌单深度自检、CSRF 自愈与排查诊断系统
- **CSRF Token 缺失自愈与 403 自动重试**：
  - 在移动端发起请求时，若当前会话缺少或失效 CSRF Token，自动向 `/csrf-token` 请求新鲜凭据并自动重试一次请求，彻底消除移动端偶发 403 Forbidden 问题。
- **界面可视化排查信息注入**：
  - 当搜索失败或无结果时，搜索框下方直接展示精细化排查诊断信息（如：`接口状态: 403 / 500 / 超时 / 0匹配`），无需电脑端 F12 即可在手机上一目了然。
- **设置面板内置「手机接口自检」工具**：
  - 在「通知与日志」中新增版本号徽章（`v2.4.5`，用于排查手机浏览器强缓存）和「测试连接」按钮，一键检测酒馆中转接口连通性与耗时。
- **移动端多重触摸与防抖支持**：
  - 搜索按键补充 `touchend` 监听与 400ms 快速连击防抖，并将按键声明为 `type="submit"`，完美适配 iOS/Android 中文虚拟键盘的搜索触发行为。

## 🌟 v2.4.4 (2026-09-12)

### 🎛️ 关闭界面与最小化界面交互重构及策略可选项
- **新增「外部空白点击行为」可选项（关闭优先 vs 最小化优先）**：
  - 在播放器设置的「显示形态」板块中，新增「外部空白点击行为」下拉配置；
  - **关闭优先（默认）**：在居中弹窗或抽屉滑出模式下，点击外部阴影遮罩空白区域，将直接关闭播放器面板（音乐后台继续播放，页面不展示悬浮球）；
  - **最小化优先**：点击外部阴影遮罩空白区域，自动将主面板缩小收纳为贴边悬浮球，支持随时在悬浮球上切歌、播暂或一键点开，适合边聊天边听歌的用户。
- **键盘 Escape 快捷逃生机制升级**：
  - 全局支持按下 `Esc` 键快捷收起界面：优先退出歌单预览/帮助子弹窗，主面板状态下遵循用户设定的「外部空白点击行为」（直接关闭或缩小为悬浮球）。
- **歌单曲目预览抽屉遮罩点击优化**：
  - 点击曲目预览抽屉外部空白遮罩层直接收起预览，体验更加自然。

## 🩹 v2.4.3 (2026-09-12)

### 📱 移动端网易云歌单搜索兼容性深度修复
- **软键盘交互与按钮点击事件加固**：
  - 去除原生表单阻止提交的硬编码，为搜索按钮注入独立触摸/点击监听器；
  - 搜索输入框全面支持手机虚拟键盘「搜索/前往」快捷确认（Enter/13），并在提交搜索时自动失去焦点收起软键盘（`blur()`），防止遮挡搜索结果。
- **跨端反向代理 Session 凭据支持**：
  - 酒馆内部中转代理 `/api/search/visit` 显式追加 `credentials: 'include'`，彻底解决手机端通过内网 IP（`192.168.x.x`）或内网穿透访问时 Session Cookie 被移动浏览器安全沙箱剥离导致的 403 Forbidden 鉴权失败。
- **网易云歌单搜索双引擎互备**：
  - 引入网易云官方 Web 搜索与 PC 云搜索（`cloudsearch/pc`）双源自动容灾降级机制，并优化 JSON 容错解析与多通道国内可用代理，保障手机端网络环境秒级出结果。

## 🩹 v2.4.2 (2026-09-12)

### 🎵 播放引擎与跨端同步修复（彻底解决双声道叠加与暂停失灵）
- **全局音频引擎互斥（Global Audio Engine Mutex）**：
  - 将底层 `Audio` 播放器挂载为顶级窗口单例（`__FIRE_AUDIO_ENGINE__`），并在任何切歌起播前强制执行硬件级 `audio.pause()`。彻底杜绝任何情况下两个音频实例重叠发声（双声道叠加）的现象。
- **单例运行防冲突守卫（Singleton Guard）**：
  - 在拓展初始化 `init()` 注入顶层窗体运行卫语句，防止酒馆在多路径或热重载时双重初始化导致两套事件监听器打架。
- **播控状态以硬件状态为基准**：
  - `togglePlayPause()` 统一以音频硬件的真实播放状态（`!audio.paused`）作为判断基准，无论在悬浮球、侧边长条还是主面板上点击，开关状态与图标准确互通，解决“按了暂停没反应”的问题。
- **DOM 残留悬浮球彻底清理**：
  - `createUI()` 重建时彻底清除旧的 `fire-float-ball` DOM 节点，避免历史球体脱节挂载。

## 🩹 v2.4.1 (2026-09-12)

### 🔮 悬浮球电脑端交互体验修复与增强
- **修复电脑端鼠标移向侧边白条时丢失悬停的 Bug**：
  - 增加 **Hover Bridge（隐形交互桥）** 伪元素，彻底消除悬浮球与侧边胶囊之间的微小缝隙死区，鼠标移动全程保持有效悬停。
  - 增加 **350ms 防抖宽容期（Hover Grace Period）**，即使指针短时间越界或快速微调，侧边控制胶囊依然稳健展开，杜绝闪烁和意外收回。
- **清除原生系统 Tooltip 遮挡**：
  - 移除悬浮球外层的原生 `title` 属性，彻底消除弹出的浏览器黑框提示对播放/下一曲/展开按钮的遮挡。
- **侧边控制条歌曲信息区交互升级**：
  - 点击侧边胶囊的歌曲名或歌手文本区域即可直接展开主播放器大窗口，电脑端操作更直观顺手。

## 🚀 v2.4.0 (2026-09-12)

### 🔍 1. 网易云歌单查找与搜索系统
- **搜单曲 / 搜歌单双模式切换**：搜索栏上方新增快捷模式切换胶囊，支持在单曲全网搜索与网易云歌单搜索之间秒级切换。
- **丰富歌单卡片与信息呈现**：搜索结果展示高清封面、播放量胶囊标签（如 `120.5万`、`3.2亿`）、歌单名称、创建者昵称及曲目总数。
- **多维度一键操作**：
  - **查看**：展开歌单曲目预览弹窗，浏览所有曲目详情。
  - **+ 追加**：一键将该歌单全部歌曲追加到当前播放列表末尾。
  - **替换**：一键替换当前播放列表并自动从头播放。
  - **收藏**：以该网易云歌单名称在本地新建同名独立歌单并持久化保存。
- **歌单搜索分页支持**：支持上一页/下一页无缝翻页。

### 🧭 2. 网易云精选歌单探索与分类推荐
- **热门与精品分类推荐**：发现页新增精选歌单模块，支持实时拉取网易云官方高热度精品歌单。
- **风格与场景多分类筛选**：内置分类胶囊标签：`全部`、`流行`、`ACG`、`古风`、`治愈`、`轻音乐`、`电子`、`摇滚`、`欧美`、`粤语`，支持秒切并带本地缓存加速。
- **实时一键刷新**：提供右上角独立刷新按钮，随时换一批新鲜高分歌单。

### ⚙️ 3. 榜单功能与发现页全面可选项化（核心需求）
- **官方排行榜功能独立开关**：
  - 设置面板「发现与榜单设置」中新增「**启用官方排行榜**」总开关（默认开启）。
  - 关闭后，发现页彻底隐藏官方榜单模块，仅保留精选歌单；设置面板中的自选展示榜单勾选器同步折叠隐藏。
- **发现功能标签页独立开关**：
  - 新增「**启用发现功能标签页**」总开关（默认开启）。
  - 如用户希望极简播放器形态，可关闭此开关，左侧导航栏的“发现”图标将彻底隐藏。

### 🎨 4. 歌单与界面 UI 视觉规范优化
- **歌单卡片功能键纯图标化**：将歌单卡片右侧的文字功能键全面改造为 `fa-list-ul`（查看）、`fa-plus`（追加）、`fa-play`（替换并播）、`fa-star`（收藏）纯图标，极大节省横向空间并避免标题截断。
- **去除冗余灰底**：移除卡片与搜索条的半透明硬黑/灰背景色，改用极简通透底色与主题边框，完美适配白色等浅色主题。
- **歌单查看抽屉顺应酒馆主题**：去除深黑半透明覆盖层，直接对接酒馆原生主题变量（`var(--SmartThemeBlurTintColor)`、`var(--fire-text)`、`var(--fire-em)`），在浅色/深色主题下均呈现自然和谐的高级质感。

### 🔮 5. 悬浮球（Floating Ball）外观精简化与贴边自动隐藏
- **外观精简为极简球体**：剥除复杂唱片机指针臂、黑胶层叠底座等冗杂结构，精简为 46×46px 圆形小巧球体，完全跟随当前酒馆主题配色。
- **松手智能贴边吸附**：悬浮球拖拽松手后自动平滑吸附至距离最近的屏幕左侧或右侧边缘（`left: 0` 或 `right: 0`），保持屏幕视界整洁。
- **2.5 秒无操作贴边自动半隐藏**：贴边静止 2.5 秒后自动向边缘缩进 55% 并半透明化（仅保留弧形微露），不阻挡正文阅读；鼠标悬停或触碰时即时 100% 弹出唤醒。

### 📑 6. 歌单曲目列表弹窗预览浮层
- 点击任何推荐歌单或搜索到的歌单卡片，即刻呼出曲目浏览抽屉。
- 完整列出歌单内所有单曲（序号、歌名、歌手），支持：
  - **单曲试听**：点击单曲旁的播放图标即可直接播放该曲。
  - **单曲添加**：点击 `+` 图标快速将单曲加入当前列表。
  - **整单播放 / 追加 / 另存为歌单**。

### 🌐 5. 跨域穿透与多源容灾引擎
- 整合 SillyTavern 本地服务端 Node.js 代理接口，配合身份凭据与 CSRF Token 穿透跨域限制，无需任何第三方外部插件。
- 具备多重接口与公开网关降级容灾，秒开秒读。

---

### 🧩 1. 「召出入口」与「显示形态」彻底解耦重构
- **独立召出入口控制**：将入口从原先的固定模式中拆分，支持独立开关：
  - **魔法棒菜单 (Extensions Menu)**：独立勾选，动态加载/卸载。
  - **快捷回复栏 (QR 栏) 注入**：独立勾选，无论何种显示形态均可使用 QR 栏图标一键呼出。
  - **斜杠命令 (`/fire`)**：独立勾选与控制。
  - **全局快捷键**：常驻支持 `Alt + M` 全局一键呼出/收起。
- **全新显示形态矩阵与比例微调**：
  - **全屏模式**：100% 沉浸大屏，支持系统状态栏避让。
  - **居中弹窗**：居中毛玻璃卡片，支持自由调节**宽度比例 (40%~95%)** 与**高度比例 (40%~95%)**。
  - **抽屉滑出 (上下左右)**：支持选择滑出方向（顶部、底部、左侧、右侧），并提供**抽屉大小占比滑块 (30%~95%)** 随心调整。
  - **自由悬浮窗**：
    - **非遮罩式**：打开后不阻塞页面交互，可一边与酒馆 AI 聊天一边切歌、看歌词。
    - **自由拖拽移动**：按住标题栏即可在屏幕任意位置拖拽，坐标自动持久化记忆。
    - **自由调整大小**：右下角带有缩放手柄，拖拽自由拉伸长宽并记忆尺寸。
    - **最小化为桌面悬浮球**：标题栏新增最小化 `-` 按钮，点击一键缩小为微型黑胶悬浮球。悬浮球支持封面旋转、播放动效、全屏拖拽与单击快速恢复面板。
- **无感兼容升级**：自动迁移旧版本 `wand-modal`、`qr-top` 等混合配置。

---

## 🚀 v2.2.4 (2026-09-12)

### ❤️ 1. 全新「我喜欢的」收藏体系
- **常驻系统歌单**：歌单下拉栏置顶呈现「❤️ 我喜欢的」，实时统计收藏曲目数量。具备系统保护机制，无法误删或重命名。
- **播放区红心交互**：正在播放歌名旁新增收藏红心按钮，实时反映当前歌曲收藏状态（实心高亮红心 / 空心红心），支持一键快速收藏或移出。
- **全场景一键收藏**：在搜索结果列表、歌单列表以及专辑详情列表中均提供专属红心收藏按钮，新收藏歌曲自动置顶排在最前。
- **双向联动与数据持久化**：本地歌单备份与恢复完美兼容，收藏操作全局即时无感同步。

---

## 🚀 v2.2.3 (2026-09-09)

### 📱 1. 移动端与全屏状态栏避让适配
- **新增「状态栏避让」可选项**：在设置面板（⚙️ 齿轮 -> 显示模式）中新增「状态栏避让」切换选项，并在更新后默认开启。
- **自动安全区适配**：在移动端竖屏高度占满或全屏模式下，自动计算安全区域（兼容 `env(safe-area-inset-top)` 与刘海/挖孔屏），为顶部标题栏与操作按钮预留出安全间隙，避免与手机电量、时钟、状态栏图标重叠遮挡。

---

## 🚀 v2.2.2 (2026-08-17)

### 🎤 1. 桌面歌词多行上下文显示
- **前后 N 条歌词自选显示**：新增「歌词显示行数」设置，支持单行模式（默认）、前后各 1 行（共 3 行）、前后各 2 行（共 5 行）、前后各 3 行（共 7 行）。
- **视觉层次分明**：当前句高亮显示（含翻译），前后上下文歌词采用半透明（`opacity: 0.55`）与适配字号呈现，平滑滚动过渡。

### 🎨 2. 桌面歌词背景条尺寸与宽距自定义
- **背景条宽度自定义**：新增「背景条宽度」调节滑块，支持自适应（280px ~ 600px）或 200px ~ 800px 自定义固定宽度。
- **背景条高度自定义**：新增「背景条高度」调节滑块，支持自适应或 30px ~ 400px 自定义固定高度，内容垂直弹性居中。
- **背景条边距 (宽距) 自定义**：新增「背景条边距 (宽距)」调节滑块（4px ~ 30px），支持将背景条缩短变窄为精致紧凑的胶囊条或舒展卡片。

---

## 🚀 v2.2.1 (2026-08-08)

### 🎵 1. 在线歌单导入与智能解析升级
- **网易云多参数 URL 精准识别**：全面支持带有 `app_version`、`userid`、`dlt` 等多参数混排的网易云歌单分享链接（如 `&id=xxx`），成功解析并导入全量曲目。
- **酷我歌单路径拓展**：新增对酷我 PC 网页版 `/playlist_detail/` 路径与移动端链接的自动解析。
- **酷狗数字 UID 歌单解析**：支持通过提取酷狗个人数字 UID（如账号 UID 或分享链接中的 `uid=569471137`），一键拉取与导入账号公开收藏歌曲。
- **双 API 智能备用与重试引擎**：加入 `fetchWithRetry` 重试机制与多 API 备用降级，修复底层 `Failed to fetch` 偶发网络抖动问题。

### 🛠️ 2. 本地 JSON 歌单备份合并修复
- **数据类型兼容去重**：修复 JSON 备份恢复合并时 `song.id` 字符串与数字类型差异导致歌曲重复添加的隐患，统一升级为 `String(ls.id) === String(song.id)` 严格匹配。

### 💡 3. UI 交互与提示优化
- **UI 下拉选项重构**：更新在线歌单导入平台菜单与占位提示，准确指引用户导入网易云/酷我歌单及酷狗数字 UID。

---

## 🚀 v2.2.0 (2026-08-06)

### 🔥 1. 网易云「发现」Tab 与全量 63 个官方排行榜
- **全新发现视角 (🔥)**：在 UI 导航栏加入「发现」Tab，可浏览与搜索网易云音乐热门榜单。
- **全量 63 个官方排行榜**：
  - **核心主打**：飙升榜、新歌榜、热歌榜、原创榜
  - **流行热度**：实时热度榜、实时分享榜、网络热歌榜、潮流风向榜、听歌识曲榜、KTV唛榜、AI歌曲榜等
  - **黑胶VIP**：黑胶VIP爱听榜、热歌榜、新歌榜、爱搜榜、限免榜
  - **细分曲风**：国风榜、中文说唱榜、全球说唱榜、电音榜、摇滚榜、民谣榜、古典榜、DJ慢摇榜、BEAT榜、R&B榜等
  - **ACG二次元**：ACG综合榜、ACG动画榜、ACG游戏榜、VOCALOID榜
  - **全球语种**：欧美热歌/新歌榜、日语榜、韩语榜、Billboard榜、UK榜、Oricon榜、俄语/泰语/越南语/法语榜
  - **车载电台**：特斯拉、理想、比亚迪、蔚来、极氪、吉利车友爱听榜
  - **合伙人与IP**：音乐合伙人系列榜、蛋仔派对听歌榜
- **极简卡片布局**：移除了内置的大图片与 Emoji，采用纯精简文本卡片布局，清晰呈现 Top 5 单曲预览。
- **一键导入与播放**：提供 **`+ 追加`** 和 **`替换`** 按钮，可将榜单快速合并/替换到当前播放队列中。

---

### ⚙️ 2. 发现榜单展示设置
- **设置面板自定义**：在设置菜单（⚙️ 齿轮 -> 发现榜单展示设置）中新增榜单选择器，支持分类勾选想看或隐藏的榜单。
- **快捷管理**：内置 **「全选」**、**「清空」**、**「恢复默认」** 一键设置工具。
- **即时无感同步**：修改勾选后即刻同步刷新发现 Tab 展示，无需重启或刷新页面。

---

### 🔀 3. 相似歌曲推荐 (Similar Songs)
- **歌名旁快捷图标**：在播放区歌名右侧新增 `<i class="fa-solid fa-shuffle"></i>` 推荐图标按钮。
- **默认隐藏，按需展开**：推荐栏默认完全隐藏，零占用播放器垂直空间，点击歌名旁图标即可平滑展开/隐藏。
- **同风格关联推荐**：自动为当前播放歌曲推荐 5 首同风格/同情绪关联歌曲，支持直接单曲播放或 **`+ 全部追加`** 续播。
- **CORS 跨域修复与高可用 Fallback**：全面解决前端直连网易云时的 CORS 同源拦截问题，配备高可用中转与回退保护，无报错日志。

---

### 🎶 4. 音源扩展与 UI 细节优化
- **新增 vkeys 腾讯音源**：支持 `vkeys_tencent` 歌曲搜索、链接解析与高品质播放，匹配专属琥珀金 Tone Badge 标识。
- **FontAwesome 图标化**：全面使用 FontAwesome 矢量图标规范系统。
- **帮助教程图标修复**：修复界面顶部教程 Help 问号图标缺失与样式截断 Bug。
